name: toosii-xd-ultra
type: worker
definition:
  name: toosii-xd-ultra
  docker:
    dockerfile: Dockerfile
  env:
    - key: NODE_ENV
      value: production
  scaling:
    min: 1
    max: 1
  instance_types:
    - nano
